#include <list>
#include <map>
#include <iostream>

class Graph 
{

	// Number of verticies
	int nv;
	//Adjacent lists for each node
	list<int> *adj_nodes;

	Graph(int v);
	bool addEdge ( int v, int w);
	bool bfs();
	bool dfs();



}
